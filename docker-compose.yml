version: '3'

services:

  frontend:
    build: ./frontend
    ports:
      - "3000:5173"

  backend:
    build: ./backend
    ports:
      - "5000:5000"

  mongodb:
    image: mongo
    ports:
      - "27017:27017"
